spring:
  cloud:
    kubernetes:
      config:
        sources:
          - name: demo
            namespace: default
          - name: demo1
            namespace: test
          - name: demo2
            namespace: develop
